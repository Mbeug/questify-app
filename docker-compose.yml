version: '3'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: questify_db
      POSTGRES_USER: questify
      POSTGRES_PASSWORD: questify
    ports: ["5432:5432"]
  backend:
    build: ./backend
    ports: ["8080:8080"]
    depends_on: [db]
